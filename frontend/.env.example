# Backend API URL
# For development, this should point to the backend server (default: http://localhost:8080)
# For production, this will be empty string as the frontend is served from the same origin
VITE_API_BASE_URL=http://localhost:8080
